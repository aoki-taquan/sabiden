# sabiden PWA フロントエンドの環境変数 (vite ビルド時に置換)
#
# シグナリング WebSocket の URL。空にすると同一オリジン (`/signal`) を使う。
# 例: wss://sabiden.example.com/signal
VITE_SIGNAL_URL=

# 開発時に `npm run dev` で `/signal` をプロキシする先のバックエンド URL。
# (vite.config.ts の server.proxy で参照)
VITE_SIGNAL_BACKEND=http://127.0.0.1:8080
